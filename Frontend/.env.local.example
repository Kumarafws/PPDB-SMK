# Auth & data penyimpanan sementara memakai mock in-memory (lihat lib/mock).
# Tidak perlu URL database untuk menjalankan proyek ini secara lokal.

# Vercel Blob — diperlukan jika upload dokumen ke storage Vercel di production
# BLOB_READ_WRITE_TOKEN=
